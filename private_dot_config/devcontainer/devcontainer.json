{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "containerEnv": {
    "AGE_SECRET_KEY": "${localEnv:AGE_SECRET_KEY}"
  },
  "postCreateCommand": "chezmoi init --apply lauritsk && (cd ~ && mise install) && mise install"
}
