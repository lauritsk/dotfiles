{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".",
    "args": {
      "BASE_IMAGE": "dhi.io/debian-base:trixie"
    }
  },
  "remoteUser": "dev",
  "containerEnv": {
    "AGE_SECRET_KEY": "${localEnv:AGE_SECRET_KEY}",
    "SHELL": "/bin/bash"
  },
  "postCreateCommand": "chezmoi init --apply lauritsk"
}
